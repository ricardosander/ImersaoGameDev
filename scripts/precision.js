class Precision {

    constructor(precisionX, precisionY, precisionWidth, precisionHeight) {
        this.x = precisionX;
        this.y = precisionY;
        this.width = precisionWidth;
        this.height = precisionHeight;
    }

}